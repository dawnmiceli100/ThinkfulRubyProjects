<div>
	<h1><%= @article.title %></h1>
	<p>Published on <%= l(@article.created_at) %></p>
	<p>Categorized under: <%= @article.categories.each do |category| %>
						<%= category.name %>&nbsp;
					<% end %>
	</p>
	<div>
		<%= @article.content %>
	</div>

</div>